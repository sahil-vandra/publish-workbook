name: PublishWorkbook
on:
  push:
    branches: [main]

env:
  UNAME: ${{secrets.USERNAME}}
  PASS: ${{secrets.PASSWORD}}
jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      changedfiles: ${{ steps.get-files.outputs.files }}

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip 
          pip install requests

      - name: Get changed files
        id: get-files
        run: echo ::set-output name=files::$(git diff-tree --no-commit-id --diff-filter=A --name-only -r $(git rev-parse HEAD) -- *.twbx | jq -R -s -c 'split("\n")[:-1]' | jq -r '.[] | (","+.)')

      - name: Print files
        run: echo "${{ steps.get-files.outputs.files }}"

      - name: Run unit tests
        run: |
          if [ ! "${{ steps.get-files.outputs.files }}" ];then
                echo "TWBX file list is Empty."
          else
                python3 publish_workbook.py --project_name "Technology" --password ${{env.PASS}} --workbook_files "${{ steps.get-files.outputs.files }}"
          fi